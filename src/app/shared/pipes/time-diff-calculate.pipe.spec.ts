import { TimeDiffCalculatePipe } from './time-diff-calculate.pipe';

describe('TimeDiffCalculatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDiffCalculatePipe();
    expect(pipe).toBeTruthy();
  });
});
